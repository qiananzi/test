<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.MemMapper">
    <select id="listMem" resultType="com.example.pojo.Mem.Mem">
        select * from mem
        <where>
            <if test="name != null">
                name like concat('%', #{name}, '%')
            </if>

            <if test="gender != null">
                and gender = #{gender}
            </if>
        </where>
        order by memid
    </select>

    <update id="update_memById" parameterType="com.example.pojo.Mem.Mem">
        UPDATE mem
        SET name   = #{name},
            gender = #{gender},
            money  = #{money},
            phone  = #{phone}
        WHERE memid = #{memID}
    </update>

    <insert id="add_mem" parameterType="com.example.pojo.Mem.Mem">
        INSERT INTO mem
            (MemID, Money, TimeIn, Name, Gender, Phone)
        VALUES (#{MemID}, #{Money}, #{TimeIn}, #{Name}, #{Gender}, #{Phone})
    </insert>
</mapper>